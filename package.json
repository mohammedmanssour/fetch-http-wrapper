{
  "name": "fetch-client",
  "version": "1.0.0",
  "description": "A Simple, Intuitive, expressive http client built on top of fetch",
  "main": "index.js",
  "scripts": {
    "build": "cross-env NODE_ENV=devlopment tsc --p tsconfig.json --outDir dist --pretty --declaration",
    "dev:watch": "cross-env NODE_ENV=devlopment tsc --outDir dist --pretty --watch"
  },
  "author": "Mohammed Manssour <manssour.mohammed@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "ava": "^1.0.1",
    "cross-env": "^5.2.0",
    "prettier": "1.15.3",
    "typescript": "^3.2.2"
  }
}
